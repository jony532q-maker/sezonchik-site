document.addEventListener('DOMContentLoaded',()=>{const grid=document.getElementById('bikeGrid'),cards=[...grid.querySelectorAll('.bike-card')],count=document.getElementById('resultCount'),search=document.getElementById('searchInput'),wheel=document.getElementById('wheelFilter'),material=document.getElementById('materialFilter'),sort=document.getElementById('sortFilter'),more=document.getElementById('loadMore');let cat='all',limit=16;const norm=s=>(s||'').toLowerCase().replace('ё','е');function apply(reset=true){if(reset)limit=16;let arr=cards.filter(c=>(cat==='all'||c.dataset.category===cat)&&(wheel.value==='all'||c.dataset.wheel===wheel.value)&&(material.value==='all'||c.dataset.material===material.value)&&norm(c.dataset.search).includes(norm(search.value)));if(sort.value==='price-asc')arr.sort((a,b)=>+a.dataset.price-+b.dataset.price);else if(sort.value==='price-desc')arr.sort((a,b)=>+b.dataset.price-+a.dataset.price);else arr.sort((a,b)=>cards.indexOf(a)-cards.indexOf(b));cards.forEach(c=>c.hidden=true);arr.slice(0,limit).forEach(c=>{c.hidden=false;grid.appendChild(c)});count.textContent=arr.length;more.hidden=arr.length<=limit;more.dataset.total=arr.length}document.querySelectorAll('[data-category]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-category]').forEach(x=>x.classList.remove('active'));b.classList.add('active');cat=b.dataset.category;apply()}));[search,wheel,material,sort].forEach(x=>x.addEventListener(x===search?'input':'change',()=>apply()));more.addEventListener('click',()=>{limit+=16;apply(false)});document.querySelectorAll('.view-btn').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.view-btn').forEach(x=>x.classList.remove('active'));b.classList.add('active');grid.classList.toggle('list-view',b.dataset.view==='list')}));document.querySelector('.menu-toggle').addEventListener('click',()=>document.querySelector('.mobile-menu').classList.toggle('open'));apply()});