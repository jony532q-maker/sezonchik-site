(() => {
 const cfg=window.SEZONCHIK_SUPABASE||{}; const configured=cfg.url&&cfg.publishableKey&&!cfg.publishableKey.includes('PASTE_');
 window.sezonchikProductsReady=(async()=>{if(!configured||!window.supabase)return false;try{const db=window.supabase.createClient(cfg.url,cfg.publishableKey);const {data,error}=await db.from('products').select('*').eq('category','bicycles').eq('published',true).order('sort_order',{ascending:true}).order('id',{ascending:true});if(error||!data?.length)return false;const grid=document.getElementById('bikeGrid');if(!grid)return false;const fmt=n=>new Intl.NumberFormat('ru-RU').format(n||0)+' ₽';const labels={adult:'Взрослый',teen:'Подростковый',child:'Детский',bmx:'BMX',stunt:'Трюковой'};const mat={aluminum:'Алюминиевая рама',steel:'Стальная рама',magnesium:'Магниевый сплав',other:'Рама'};const br={hydraulic:'Гидравлические тормоза',disc:'Дисковые тормоза',rim:'Ободные тормоза',other:'Тормоза'};grid.innerHTML=data.map(p=>{const specs=[p.wheel_size?`${p.wheel_size}”`:'',mat[p.frame_material]||'',p.speeds?`${p.speeds} скорость`:'',br[p.brakes]||''].filter(Boolean);const tg='https://t.me/sezonchik_dv?text='+encodeURIComponent(`Здравствуйте! Интересует ${p.title}. Подскажите актуальное наличие и цену.`);return `<article aria-label="Открыть ${p.title}" class="bike-card" data-availability="${p.in_stock?'stock':'order'}" data-brake="${p.brakes||'other'}" data-category="${p.product_type||'adult'}" data-href="${tg}" data-material="${p.frame_material||'other'}" data-price="${p.price||0}" data-search="${[p.title,p.description,...specs].join(' ')}" data-speed-group="${p.speeds>1?'multi':'single'}" data-speeds="${p.speeds||''}" data-wheel="${p.wheel_size||''}" role="link" tabindex="0"><div class="bike-media"><div class="card-badges"><span class="fact-badge ${p.in_stock?'in-stock':'on-request'}">${p.in_stock?'В наличии':'Наличие уточняйте'}</span>${p.bestseller?'<span class="fact-badge">Хит продаж</span>':''}${p.featured?'<span class="fact-badge">Новинка</span>':''}</div><img alt="${p.title}" decoding="async" loading="lazy" src="${p.image||'assets/category-bicycles-new.webp'}"><div class="card-tools"><button aria-label="Увеличить фото" class="card-tool zoom-btn" type="button">⛶</button><button aria-label="Поделиться" class="card-tool share-btn" type="button">↗</button></div></div><div class="bike-info"><span class="bike-type">${labels[p.product_type]||'Велосипед'}</span><h2>${p.title}</h2><div class="bike-specs">${specs.map(x=>`<span>${x}</span>`).join('')}</div><div class="bike-bottom"><strong class="bike-price">${fmt(p.price)}</strong><a class="bike-status ${p.in_stock?'':'status-order'}" href="${tg}" target="_blank" rel="noopener">${p.in_stock?'В наличии в магазине':'Наличие уточняйте'}</a></div><label class="compare-check"><input aria-label="Добавить в сравнение" class="compare-input" type="checkbox"> Сравнить</label><span class="card-open-hint">Уточнить модель →</span></div></article>`}).join('');document.getElementById('resultCount').textContent=data.length;return true}catch(e){console.warn('Supabase fallback:',e);return false}})();
})();
